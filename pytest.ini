[pytest]
asyncio_default_fixture_loop_scope = function
pythonpath = src
minversion = 6.0
addopts = --cov=src --cov-report=xml --cov-report=html -ra -q
testpaths =
    tests
markers =
    integration: marks tests as integration tests (can be slow)
    # Add other custom markers here if needed 